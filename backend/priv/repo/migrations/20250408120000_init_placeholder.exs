defmodule DiagramaSavana.Repo.Migrations.InitPlaceholder do
  use Ecto.Migration

  @doc """
  Placeholder migration so `ecto.migrate` has a baseline.

  Domain tables (carteira, ativos, etc.) will be added in later steps.
  """
  def change do
  end
end
